import React from 'react';
import styles from '@/styles/NotFound.module.css'

const NotFoundPage = () => {
    return (
        <div>
            not found
        </div>
    );
};

export default NotFoundPage;