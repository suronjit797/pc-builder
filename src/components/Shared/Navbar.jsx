import React from 'react';
import { useSession, signIn, signOut } from "next-auth/react"

const Navbar = () => {
    return (
        <div>
            navbar
        </div>
    );
};

export default Navbar;